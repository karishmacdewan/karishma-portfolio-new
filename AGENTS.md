# Repository working preferences

- For visual or UI behavior changes, implement the requested change and run static checks only. Do not launch a browser, start the app for visual inspection, or perform visual verification unless the user explicitly asks; the user will verify the rendered behavior.
