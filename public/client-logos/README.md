# Client logo assets

The marquee currently uses the supplied assets for all nine brands.

SVG is preferred, but the file paths in `CompanyMarquee.tsx` can be changed to `.png` if needed.
Use transparent variants intended for dark backgrounds so black details remain visible.
